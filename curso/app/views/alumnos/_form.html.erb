<%= form_with model: @alumno, local: true do |form| %>
 
  <% if @alumno.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@alumno.errors.count, "error") %> prohibited
        this alumno from being saved:
      </h2>
      <ul>
        <% @alumno.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= form.label :nombre %><br>
    <%= form.text_field :nombre %>
  </p>
 
  <p>
    <%= form.label :rut %><br>
    <%= form.text_field :rut %>
  </p>

  <p>
    <%= form.submit %>
  </p>
      
<% end %>